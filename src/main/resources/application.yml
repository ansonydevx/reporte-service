server:
  port: 8085

spring:
  application:
    name: reporte-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/reportes_db

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
